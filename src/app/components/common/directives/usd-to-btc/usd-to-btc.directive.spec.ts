import { UsdToBtcDirective } from './usd-to-btc.directive';

describe('UsdToBtcDirective', () => {
  it('should create an instance', () => {
    const directive = new UsdToBtcDirective();
    expect(directive).toBeTruthy();
  });
});
